apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rental-gateway
spec:
  rules:
    - host: rental.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway-service
                port:
                  number: 80